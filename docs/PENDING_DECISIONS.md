# 논의 대기 결정 (Pending Decisions)

> **이 문서는 무엇인가** — 개발이 **단독으로 판정하지 못하는** 것만 모은다. 백엔드 스펙·기획/제품 판단·
> 디자인 판단처럼 **팀의 다른 축이 확정해줘야 닫히는** 항목들이다. "모아서 논의"하기 위한 단일 창구.
>
> **`docs/TODO.md` 9절과의 차이** — TODO 9절은 이미 **되살리기/고치기로 결정된 것**(구현만 남음)이다.
> 여기 있는 건 **결정 자체가 열려 있어** 개발이 임의로 정하면 안 되는 것. (출처: 이슈 **#222** C1/C2 판정 세션 2026-08-28)
>
> **닫는 방법** — 각 항목의 "닫는 조건"이 충족되면 → 결정을 해당 모듈 `V1_BEHAVIOR_CONTRACT.md`와
> (구현이 필요하면) `docs/TODO.md` 9절에 반영하고 여기서 제거한다.

---

## 요약

| # | 항목 | 모듈 | 담당(확정 주체) | 근거 |
|---|---|---|---|---|
| 1 | 탈퇴 요청 body에 `refreshToken`이 필요한가 | Setting | **백엔드** | 계약서 6.1 · C2 3 |
| 2 | 상세검색 플랫폼 서버값 "리디북스"를 서버가 수용하는가 | Search | **백엔드** | 계약서 부록 · C2 5 |
| 3 | 서재 `.title`(제목순) 정렬 서버 토큰을 서버가 지원하는가 | Library | **백엔드** | 계약서 1.4 |
| 4 | 비공개 프로필에서 서재 통계 숫자를 노출할지 | UserPage | **기획·제품** | 계약서 4.7 |
| 5 | 감상평 첫 진입 1회성 온보딩 힌트를 되살릴지 | NovelDetail | **디자인** | 계약서 4.x |
| 6 | 비로그인/온보딩 분석 계측을 어디서 이어받을지 | Onboarding | **기획**(Amplitude 횡단과 연동) | 계약서 1.2 |

---

## A. 백엔드 확인 필요

### 1. 탈퇴 `/auth/withdraw` body에 `refreshToken`이 필요한가 — Setting

- **V1**: `POST /auth/withdraw` body에 `{ reason, refreshToken }`를 실었다.
- **V2 현행**: body에 `{ reason }`만 싣고 refreshToken을 빼며, 인증은 헤더 access 토큰으로 한다.
- **왜 논의**: V2 서버 스펙이 body의 refreshToken을 **요구하는지 아닌지**에 따라 현행이 맞을 수도(불요구), 회귀일 수도(요구) 있다. 클라이언트가 임의로 판단 못 한다.
- **닫는 조건**: 백엔드가 "탈퇴는 헤더 access 토큰만으로 처리, body refreshToken 불요"를 확정하면 **현행 유지**로 닫는다. 요구한다면 body에 refreshToken을 다시 싣는 수정을 TODO 9절로.
- **근거**: `Projects/Feature/SettingFeature/V1_BEHAVIOR_CONTRACT.md`(탈퇴 절, ⏳ 보류 배지) · `docs/V1_PARAM_MAPPING_C2.md`(0절 3번)

### 2. 상세검색 플랫폼 서버값 "리디북스"를 서버가 수용하는가 — Search

- **V1/V2 공통 의도**: UI 라벨은 "리디"지만 서버로는 **"리디북스"**를 보낸다(#185, 사용자 확정). `SearchMapper.mapNovelPlatformString`.
- **왜 논의**: 표기 불일치는 의도된 것이지만, **서버 enum이 "리디북스"를 실제로 수용**하는지 서버값 대조가 안 됐다. 서버가 "리디"만 받으면 필터가 빈 결과가 된다.
- **닫는 조건**: 백엔드에서 플랫폼 enum 목록을 받아 "리디북스"(및 나머지 4개 한글 표기) 수용을 확인하면 닫는다.
- **근거**: `Projects/Feature/SearchFeature/V1_BEHAVIOR_CONTRACT.md`(부록 A) · `Projects/Data/SearchData/CLAUDE.md`(상세탐색 필터 절) · `docs/V1_PARAM_MAPPING_C2.md`(0절 5번)

### 3. 서재 `.title`(제목순) 정렬 서버 토큰을 서버가 지원하는가 — Library

- **V1**: 제목순 정렬 토큰(`"title"`)에 *"TODO: 백엔드 토큰 확정 필요(v2 스펙 미정의)"* 주석이 달려 있었다 — V1 시점에도 미확정.
- **V2 현행**: 매퍼가 토큰을 싣지만, 서버가 제목순 정렬을 **실제 지원하는지**는 확인 안 됨.
- **왜 논의**: 서버가 미지원이면 제목순 정렬이 무동작(또는 기본 정렬로 폴백)한다. V1도 못 풀고 넘긴 것이라 백엔드 확정이 선행돼야 한다.
- **닫는 조건**: 백엔드가 제목순 정렬 토큰의 정식 값·지원 여부를 확정하면 매퍼 토큰을 맞추고 닫는다.
- **근거**: `Projects/Feature/LibraryFeature/V1_BEHAVIOR_CONTRACT.md`(1.4 정렬)

---

## B. 기획·제품 판단 필요

### 4. 비공개 프로필에서 서재 통계 숫자를 노출할지 — UserPage

- **V1**: 타유저 프로필이 비공개여도 **서재 통계(관심/보는중/봤어요/하차 개수)는 private 게이팅 밖**이라 숫자가 노출됐다.
- **V2 현행**: `isProfilePrivate`면 통계 포함 탭 전체를 **"비공개" 안내로 덮는다**.
- **왜 논의**: "비공개인데 숫자는 보임"(V1)이 맞는지 vs "통째로 가림"(V2)이 맞는지는 **제품·기획 정책**이다. 프라이버시 정책 판단이라 개발이 못 정한다.
- **닫는 조건**: 기획이 노출 정책을 확정하면 해당 방향으로 맞추고 닫는다.
- **근거**: `Projects/Feature/UserPageFeature/V1_BEHAVIOR_CONTRACT.md`(4.7, ⏸ 보류 배지)

---

## C. 디자인 판단 필요

### 5. 감상평 첫 진입 1회성 온보딩 힌트를 되살릴지 — NovelDetail

- **V1**: 감상평을 **처음 볼 때 1회성 온보딩 오버레이**(딤 + 상태바 미리보기 + 말풍선 힌트)를 띄우고, 닫으면 `UserDefaults`에 저장해 다시 안 띄웠다.
- **V2 현행**: 없다.
- **왜 논의**: 재도입 여부는 **디자인 판단**(첫 사용자 안내가 지금 UI에도 필요한가)이다. 기능 회귀라기보다 UX 결정.
- **닫는 조건**: 디자인이 "재도입/생략"을 정하면 반영하고 닫는다(재도입 시 구현은 TODO 9절로).
- **근거**: `Projects/Feature/NovelDetailFeature/V1_BEHAVIOR_CONTRACT.md`(감상평 첫 진입 힌트 절, 🔧 재도입 검토 배지)

---

## D. 분석 계측 정책 (기획 — Amplitude 횡단과 연동)

### 6. 비로그인/온보딩 분석 계측을 어디서 이어받을지 — Onboarding

- **V1**: "둘러보기"(비로그인) 진입 등에 `AmplitudeManager.track(...nonLogin)` 이벤트를 심었다.
- **V2 현행**: Amplitude 의존이 없고(외부 의존성 없음 원칙) 게스트 경로 자체도 없어 이벤트가 사라졌다.
- **왜 논의**: 온보딩/비로그인 계측을 **어디서 이어받을지**(다른 계측 수단·시점)는 앱 전반 애널리틱스 정책이라 개별 화면이 못 정한다. `docs/TODO.md` 9절의 **"Amplitude 애널리틱스 횡단 재도입"**(별도 이슈 승격 대상)과 함께 판단해야 한다.
- **닫는 조건**: 횡단 애널리틱스 재도입 방침이 서면 온보딩 계측 지점을 그 안에서 정하고 닫는다.
- **근거**: `Projects/Feature/OnboardingFeature/V1_BEHAVIOR_CONTRACT.md`(1.2 둘러보기·비로그인) · `docs/TODO.md`(9절 Amplitude 횡단)
