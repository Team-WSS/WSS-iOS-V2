<!--
새 모듈 가이드 작성법:
  1. 이 내용을 복사해 `Projects/<레이어>/<모듈명>/CLAUDE.md`로 만든다 (co-location → 해당 모듈 작업 시 자동 로드).
  2. 구성요소 나열은 하지 않는다. 코드/디렉토리만 봐도 아는 정보는 적지 않는다.
  3. "코드만 봐선 모르는 것"만 적는다: 핵심 시나리오, 숨은 의존, 함정, 왜 이렇게 했는지.
  4. 함정이 없으면 짧아도 된다. 빈 문서를 위해 억지로 채우지 말 것.
-->
<!-- 모듈 가이드. 이 모듈 작업 시 상위 Projects/<레이어>/CLAUDE.md(레이어 규칙)와 함께 자동 로드됨. -->
# <모듈명>

(한 줄 책임). 구성요소는 `Sources/`를 직접 보면 된다.

- 식별자: `ModuleType.<layer>(.<case>)` / 의존: (비자명한 의존만)
- 진입점: (있으면. 예: Data의 Factory)

## 핵심 시나리오

- (외부에서 어떻게 진입하는지, 여러 타입이 얽히는 흐름 2~3개)

## 주의사항 (작업 중 발견 시 누적)

- (이 모듈 특유의 함정·예외·TODO)
